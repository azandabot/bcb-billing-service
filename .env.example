# Port the HTTP server listens on
PORT=3000

# Default per-transaction fee in GBP, applied when POST /currencies omits transactionFeeGbp.
# Resolved once at currency registration time so existing tariffs are unaffected by changes here.
TRANSACTION_FEE_GBP=0.30
